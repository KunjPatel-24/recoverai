# RecoverAI backend
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
python-dotenv>=1.0
httpx>=0.27          # used for live Razorpay Test Mode (offline demo needs no key)

# Postgres only. SQLite (the default) needs nothing extra; uncomment this
# BEFORE pointing DATABASE_URL at a postgresql:// URL, or the app fails to
# boot with "No module named 'psycopg2'".
# psycopg2-binary>=2.9
